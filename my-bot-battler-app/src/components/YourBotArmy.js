import React from "react";

function YourBotArmy ( {bots} ){ 
//  console.log(bots)
//     let display = bots
// let individualBot = bots.filter((bot)=>{return bot.id === itemId})
    // bots.filter((bot) => {if( bot === `${bot.id}`){return display}} )
//    [{
//     ID : 159
//    }]
    return (
                <div className="bot-display">
            
                <div id='mybotarmy'>

                <h1>The Recruite Bench</h1>

                </div>

            </div>
            )
        }

export default YourBotArmy;